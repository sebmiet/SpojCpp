#include <iostream>
#include <cstdlib>
#include <algorithm>

using namespace std;

int main()
{
int proby;
string napis, tesame;

 cin>>proby;
    for(int i=1; i<=proby; i++)
    {
        cin>>napis;
        int dltxt = napis.length();
        for(int i=0; i<=dltxt; i++)
        {
            if(napis[i]==napis[i+1] && napis[i+2])
            {
                int i = atoi(tesame.c_str());

            }
            cout<<i;
        }
    }


}
